<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.wcu.cs.catamountcharacters">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenu"
            android:label="Main Menu" />
        <activity android:name=".OneLetter"
            android:label="One Letter" />
        <activity android:name=".TwoLetters"
            android:label="Two Letters" />
        <activity android:name=".MyImage"
            android:label="My Image" />
        <activity android:name=".Letters"
            android:label="Letters" />
        <activity android:name=".Info"
            android:label="Info" />

    </application>

</manifest>